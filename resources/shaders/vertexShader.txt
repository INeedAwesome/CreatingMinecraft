//VERTEX SHADER
#version 400 core

in vec3 position;
in vec3 color;

out vec3 colour;

uniform mat4 projectionMatrix;

void main(void) {

    gl_Position = projectionMatrix * vec4(position, 1);
    colour = color;

}